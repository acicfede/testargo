apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kserve-pvc
  namespace: kserve-demo
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: ''
